const keyBase = `io.loopback.dataloader.decorator`;

export const DataloaderClassKey = `${keyBase}.class.DATALOADER_CLASS`;
export const DataloaderParameterKey = `${keyBase}.parameter.DATALOADER_PARAMETER`;
